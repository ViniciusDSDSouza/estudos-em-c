#include <stdio.h>

int main() {
    // Operações relacionais servem para saber se algo é verdadeiro (1) ou falso (0)
    // 1 = true;
    // 0 = false
    // Operadores
    // (>) maior que
    // (>=) maior ou igual
    // (<) menor que
    // (<=) menor ou igual
    // (==) igual
    // (!=) diferente

    int a = 1;
    int b = 0;

    printf("%i" , a > b);

    return 0;
}